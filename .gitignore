# Secrets and local env — only the root docker-compose .env is ignored.
# app/.env follows Symfony convention (defaults committed; secrets live in .env.local).
/.env
.env.*.local

# Dev tooling
.tasks-log.csv
.claude/settings.local.json
.idea/
.vscode/
*.log

# Python (sidecar)
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node (tests/tooling)
node_modules/

# Playwright
test-results/
playwright-report/
